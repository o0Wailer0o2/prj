server:
  port: ${BACKEND_PORT:8080}

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3306/julytus?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true}
    username: root
    password: password

logging:
  level:
    org:
      jooq: DEBUG
