<configuration>

    <!-- Cho phép %clr, %highlight -->
    <conversionRule conversionWord="clr"
                    converterClass="org.springframework.boot.logging.logback.ColorConverter" />
    <conversionRule conversionWord="wex"
                    converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter" />
    <!-- Converter emoji -->
    <conversionRule conversionWord="emojiLevel"
                    converterClass="vn.backend.core.config.log.EmojiLevelConverter" />

    <property name="CONSOLE_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss,Asia/Ho_Chi_Minh} %highlight(%emojiLevel) %clr([%logger{36}]){cyan} : %msg%n" />

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${CONSOLE_LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
